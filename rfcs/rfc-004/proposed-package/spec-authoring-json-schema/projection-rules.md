# JSON Schema Projection Rules — RELOCATED

> **This document has moved.** As of RFC-035 (Change G), the normative source→JSON-Schema projection contract
> lives at **[`docs/schema/2.0/projection-rules.md`](../../../../docs/schema/2.0/projection-rules.md)**, re-anchored
> from RFC-004's superseded `schema-definition`/`schema-member` vocabulary to the self-hosted `Field`/`Type`
> meta-model (RFC-032 `fieldType` model).
>
> RFC-004 is **Superseded** (RFC-033 Change E); this file and the rest of `rfcs/rfc-004/proposed-package/` remain
> only as historical design material. Do not treat the content that previously lived here as current — the live
> contract is the relocated document.
