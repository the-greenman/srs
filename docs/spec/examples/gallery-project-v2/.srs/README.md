This directory is the `.srs` repository marker for this example.

Its contents are implementation-private (see `ext:repository` in the SRS
specification) and carry no normative weight. This placeholder exists only
so the marker directory survives storage and archive round-trips that do
not preserve empty directories.

The real "About SRS" orientation content for this file is deferred to
[srs#340](https://github.com/the-greenman/srs/issues/340). For authoritative
information about this repository, see `manifest.json` and the records
under `records/` — never this file.
