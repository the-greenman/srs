{
  "$comment": "Known violations of scripts/check-spec-coherence.mjs (srs#560), each citing the live issue that removes it. Entry shape: {check, id | pair:[sourceId,targetId], issue, note}. The check fails on any violation NOT listed here and on any entry that no longer matches a violation \u2014 shrinking this file IS the migration (#562 un-bakes headings, #563 wires the tree, #564 makes invariants leaves). Growing it needs an issue. `note` is for the human reader only; the key is check + id/pair.",
  "entries": [
    {
      "check": "no-orphan-leaf",
      "id": "5b507597-f824-576b-b577-fd8141f1eaf1",
      "issue": "#710",
      "note": "Distribution Group (Core): retired by succession into the new concept \"Distribution Group (Core)\" (8888b7de), which carries a derived-from edge to it and took over its Part-contains position (RFC-042 Change B). Kept as a catalogued record (not deleted) because no identity-preserving retype tool exists yet (srs#178, OPEN) and the derived-from relation needs a live target. Excluded from publication reachability for the same reason (scripts/publication-reachability-exclusions.json)."
    },
    {
      "check": "no-orphan-leaf",
      "id": "6cb78382-35a5-5acc-a26c-774a2c288e23",
      "issue": "#710",
      "note": "Conversation Layer: retired by succession into the new concept \"Conversation Layer\" (c34c0dd8), which carries a derived-from edge to it and took over its Part-contains position (RFC-042 Change B). Kept as a catalogued record (not deleted) because no identity-preserving retype tool exists yet (srs#178, OPEN) and the derived-from relation needs a live target. Excluded from publication reachability for the same reason (scripts/publication-reachability-exclusions.json)."
    },
    {
      "check": "no-orphan-leaf",
      "id": "a1fc7f7c-dcba-5f4b-b9fd-04f91b2762b3",
      "issue": "#710",
      "note": "Extension Interactions: retired by succession into the new concept \"Extension Interactions\" (4d1fa8d8), which carries a derived-from edge to it and took over its Part-contains position (RFC-042 Change B). Kept as a catalogued record (not deleted) because no identity-preserving retype tool exists yet (srs#178, OPEN) and the derived-from relation needs a live target. Excluded from publication reachability for the same reason (scripts/publication-reachability-exclusions.json)."
    },
    {
      "check": "no-orphan-leaf",
      "id": "5bc9b930-462d-535d-91c5-bd9e2b17c91c",
      "issue": "#691",
      "note": "Namespace Format: retired by succession into the merged \"Notational conventions\" mechanism (b3c293f9), which carries a derived-from edge to it. Kept as a catalogued record (not deleted) because no identity-preserving retype tool exists yet (srs#178, OPEN) and the derived-from relation needs a live target. Excluded from publication reachability for the same reason (scripts/publication-reachability-exclusions.json)."
    },
    {
      "check": "no-orphan-leaf",
      "id": "63065301-6afc-5ae6-8d62-59cae1a3154c",
      "issue": "#691",
      "note": "Schema Notation: retired by succession into the merged \"Notational conventions\" mechanism (b3c293f9), which carries a derived-from edge to it. Kept as a catalogued record (not deleted) because no identity-preserving retype tool exists yet (srs#178, OPEN) and the derived-from relation needs a live target. Excluded from publication reachability for the same reason (scripts/publication-reachability-exclusions.json)."
    }
  ]
}